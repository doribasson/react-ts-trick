מזה ?? Double questions marks
ב js.

זה כמו || רק מחמיר יותר.

const foo = 0 ?? 'default string';
console.log(foo);

לוקח את שני המתשנים ובודק אם המשתנה השמאלי הוא לא null ולא undefined. אם לא הוא יקח את המשתנה השמאלי אחרת יקח את המשתנה הימני.

לעמת || שמחמיר יותר הוא יבדוק בנוסף שהמשתנה השמאלי הוא לא 0 ולא null ולא undefined ולא "" אם הוא לא אחד מאלה הוא יקח את המשתנה השמאלי אחרת הוא יקח את המשתנה הימני.